<!DOCTYPE html>
<html lang="ja">
<head>
   <styles type="text/css" media="all" href="books">
   <title>BookersIndex</title>
   <meta charset="UTF-8">
</head>

<body>
   <p id="notice"></p>
   <h1>Books</h1>
   
   <table>
      
      <thead>
         <tr>
            <th>Title</th>
            <th>Body</th>
            <th colspan=""></th>
         </tr>
      </thead>
      
      <tbody>
         <% @books.each do |book| %>
         <tr>
            <th><%= book.title %></th>
            <th><%= book.body %></th>
            <th><%= link_to 'show', book_path(book) %></th>
            <th><%= link_to 'edit', edit_book_path(book) %></th>
            <th><%= link_to 'Destroy', book_path(book), method: :delete %></th>
         </tr>
         <% end %>
      </tbody>
      
   </table>
   
   <h2>New book</h2>
   <form>
      <%= form_with model:@book,local:true do |f| %>
      
        <div class="field">
          <label for="book_title">Title</label>
          <%= f.text_field :title %>
        </div>
      
        <div class="field">
          <label for="book_body">Body</label>
          <%= f.text_area :body %>
        </div>
      
        <div class="actions">
          <%= f.submit 'Create Block' %>
        </div>
     <% end %>
   </form>
</body>
</html>